{
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "check": "pnpm run format:check && pnpm run lint",
    "format": "pnpm run format:rs && pnpm run format:oxk",
    "format:rs": "cargo fmt --all",
    "format:oxk": "oxk format \"**/*.{ets,js,ts,json5}\" -t=10",
    "format:check": "sh scripts/format-check.sh",
    "lint": "pnpm run lint:oxk && pnpm run lint:rs",
    "lint:oxk": "oxk lint --config .oxlintrc.jsonc . --threads=10",
    "lint:rs": "pnpm run lint:rs:aarch64 && pnpm run lint:rs:armv7 && pnpm run lint:rs:x86_64",
    "lint:rs:aarch64": "cargo clippy --workspace --exclude webview_example --exclude xcomponent_example --target aarch64-unknown-linux-ohos --all-targets --all-features -- -D warnings",
    "lint:rs:armv7": "cargo clippy --workspace --exclude webview_example --exclude xcomponent_example --target armv7-unknown-linux-ohos --all-targets --all-features -- -D warnings",
    "lint:rs:x86_64": "cargo clippy --workspace --exclude webview_example --exclude xcomponent_example --target x86_64-unknown-linux-ohos --all-targets --all-features -- -D warnings",
    "prek": "prek run",
    "pre-commit": "prek run",
    "prek:all": "prek run --all-files",
    "prepare": "prek install && cargo install ohpm-cli",
    "pack": "ohpm-rs pack --workspace"
  },
  "devDependencies": {
    "@j178/prek": "^0.4.5",
    "@ohos-rs/oxk": "^0.7.1"
  }
}
